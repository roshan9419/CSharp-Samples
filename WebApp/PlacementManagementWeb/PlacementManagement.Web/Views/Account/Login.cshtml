@using PlacementManagement.Web.ViewModels
@model LoginViewModel
@{
    ViewBag.Title = "Log in";
}

<div class="row min-vh-100 d-flex justify-content-center align-items-center" style="background-color: royalblue">
    <div class="col-md-8 d-flex flex-column align-items-center">
        <h2 class="text-center mb-3 display-4 text-light">PMS</h2>
        <h3 class="mb-5 text-light">Placement Management System</h3>
        <section id="loginForm" class="card shadow p-4" style="width: fit-content">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { style = "width: fit-content; min-width: 400px", role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(m => m.UserName, new { @class = "control-label ml-3" })
                    <div class="col-md-12">
                        @Html.TextBoxFor(m => m.UserName, new { @class = "form-control col-12" })
                        @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Password, new { @class = "control-label ml-3" })
                    <div class="col-md-12">
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control col-12"})
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="ml-3">
                        <div class="checkbox">
                            @Html.CheckBoxFor(m => m.RememberMe)
                            @Html.LabelFor(m => m.RememberMe)
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-12">
                        <input type="submit" value="Log in" class="btn btn-primary w-100 py-2 col-12" />
                    </div>
                </div>
            }
        </section>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}