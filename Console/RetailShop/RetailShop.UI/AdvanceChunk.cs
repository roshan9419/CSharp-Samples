namespace RetailShop.UI
{
    internal class AdvanceChunk<T> : BasicChunk<T>
    {
        internal static T[] MoveChunk(T[] chunk)
        {
            return chunk;
        }

        internal static T[] SortChunk(T[] chunk)
        {
            return chunk;
        }
    }
}